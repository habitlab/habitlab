description: Prevents the user from scrolling after a certain amount of scrolls.
matches:
  - www.facebook.com
content_scripts:
  - frontend.js
parameters:
  - name: scrollevents
    description: The amount of scroll events until the notification appears again
    default: 750
    type: int
  - name: debug
    description: Insert debug console
    default: false
    type: bool