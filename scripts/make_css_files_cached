#!/usr/bin/env node
// Generated by LiveScript 1.5.0
(function(){
  var fs, CleanCSS, options, cssmin, list_of_files_to_make_cached, output, i$, len$, filename;
  fs = require('fs');
  CleanCSS = require('clean-css');
  options = {
    compatibility: '*'
  };
  cssmin = new CleanCSS(options);
  list_of_files_to_make_cached = ['bower_components/sweetalert2/dist/sweetalert2.css', 'modules_custom/jquery.terminal/css/jquery.terminal.min.css'];
  output = {};
  for (i$ = 0, len$ = list_of_files_to_make_cached.length; i$ < len$; ++i$) {
    filename = list_of_files_to_make_cached[i$];
    output[filename] = cssmin.minify(fs.readFileSync('src/' + filename, 'utf-8')).styles;
  }
  fs.writeFileSync('src/libs_common/css_files_cached.js', 'module.exports = ' + JSON.stringify(output) + ';');
}).call(this);
